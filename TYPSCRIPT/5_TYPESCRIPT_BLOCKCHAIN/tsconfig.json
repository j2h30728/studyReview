{
  "include": ["src"], // 타입스크립트가 위치하는
  "compilerOptions": {
    "outDir": "build", //자바스크깁트 파일이 생성될 디렉터리
    "target": "ES6", // 컴파일할 자바스크립트 버전
    "lib": ["ES6", "DOM"], //DOM :브라우저에서 실행 -> document,localStorage, Math, window를 썼을때 무엇인지 알고 자동완성기능 제공
    "strict": true,
    "allowJs": true
  }
}
